name: First workflow

on:
  push:
    branches: 
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: Actions/checkout@v3
        
      - name: Say hello
        run: echo "Hello, world!"
